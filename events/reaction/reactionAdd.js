const { MessageEmbed } = require("discord.js");
const LOGGER = require("../../utils/ModLogger");

module.exports = {
  name: "reactionAdd",
  description: "this is event for add reaction",
  async run(client, reaction, user) {
    //
    
  },
};
